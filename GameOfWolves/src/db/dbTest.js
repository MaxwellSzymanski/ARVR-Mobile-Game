const insertUser = require("./insertUser.js");
const verifyUser = require("./verifyUser.js");

/*
const testUser = {
    email: 'test@test.be',
    password: 'verySafePassword',
    name: 'name'
};

insertUser(testUser);
*/

/*
async function verif(user) {
    const bool = await verifyUser(user);
    console.log("test: " +await bool);
    if (await bool) console.log('got this far');
    return await bool;
}

const verifyTestUser = {
    email: 'test@test.be',
    password: 'verySafePassword'
};

verif(verifyTestUser);
*/
